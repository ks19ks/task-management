<p id="notice"><%= notice %></p>
<p>
  <strong><%= t('activerecord.attributes.task.title') %>: </strong>
  <%= @task.title %>
</p>
<p>
  <strong><%= t('activerecord.attributes.task.description') %>: </strong>
  <%= @task.description %>
</p>
<p>
  <strong><%= t('activerecord.attributes.task.duedate') %>: </strong>
  <%= l(@task.due_date) if @task.due_date %>
</p>
<%= link_to t('.edit'), edit_task_path(@task) %> |
<%= link_to t('.back'), tasks_path %>
